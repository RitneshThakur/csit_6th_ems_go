for remote access token 
github-settings-developer_setting_personal_access_token(classics)-genberate with the permisiion
then https thne addd "token@github//repo"
in ide 
git config user.name
git config user.email
